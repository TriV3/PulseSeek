[package]
name = "pulseseek-decoder-symphonia"
version = "0.1.0"
edition = "2021"
license = "MPL-2.0"

[dependencies]
pulseseek-domain = { path = "../pulseseek-domain" }
symphonia = { version = "0.5", default-features = false, features = ["wav", "pcm"] }

[dev-dependencies]
tempfile = "3"
